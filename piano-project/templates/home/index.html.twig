{% extends 'base.html.twig' %}
{% block title %} Hello ! {% endblock %}
{% block body %}
<br/><br/><br/><br/><br/>
<div class="row justify-content-center">
	<h1 onclick="interval()" id="note" style="font-size:150px;">Go</h1>
</div>
<script type="text/javascript">
	function getRandomInt(max) {
	  return Math.floor(Math.random() * Math.floor(max));
	}

	function interval() {
		intervalID = setInterval(random,5000);
	}
	function random() {
		console.log('ok')
		let tab = ['Do', 'Do#', 'Re', 'Re#', 'Mi', 'Fa', 'Fa#', 'Sol', 'Sol#', 'La', 'La#', 'Si'];
		let note = document.getElementById('note');
		note.innerHTML = tab[getRandomInt(tab.length)];
	}
</script>
{% endblock %}
