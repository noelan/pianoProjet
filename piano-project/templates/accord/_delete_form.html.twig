<form method="post" action="{{ path('accord_delete', {'id': accord.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ accord.id) }}">
    <button class="btn">Delete</button>
</form>
